# 调优报告（工作包 30）

> 自对弈基线数据、发现的差距、改进待办。命令：
> `python3 scripts/self_play.py --seeds 5 --opponent <idle|scripted>`

## 一、当前基线（commit HEAD，全 1300 回合）

| 对手 | 对局 | 胜 | 负 | 平 | 胜率 | 平均总分 |
| --- | --- | --- | --- | --- | --- | --- |
| `idle`（空策略） | 5 | 5 | 0 | 0 | 1.00 | 82.8 |
| `scripted`（模拟器朴素防御 Bot） | 5 | 3 | 2 | 0 | 0.60 | 89.2 |

> 对照：加入「白天后段回防」前为 **0/5 胜**、均分 86.2；该改动后 **3/5 胜**。

## 二、发现

1. **能稳赢空 Bot**，加入回防后**对模拟器 ScriptedBot 也能过半取胜**（3/5）。
2. **平均总分仍偏低（~83–89）**：满生存分应为 550（10 天），说明基地常在数天内被
   机器人浪潮摧毁——**存活时长仍是最大短板**。
3. 机器人浪潮随天数增长；仅 3 座 1 级武器 + 无围墙，后期仍会被淹没。

## 三、已落地的调优

| 改动 | 效果 |
| --- | --- |
| **白天后段回防**（`economy.DUSK_RETURN=40`：第 40 回合起工人撤回基地） | 保证夜晚有操控者，对 scripted 胜率 0/5 → 3/5 |

## 四、已试未采纳（保留记录）

| 改动 | 结果 | 处置 |
| --- | --- | --- |
| **建围墙**（武器建满后黄区补墙，耗背包石头） | 中性：vs scripted 仍 3/5、均分 89.2→87.8；vs idle 5/5、均分 82.8→83.6 | 已回退（未达明确增益，保持代码精简） |

## 五、改进待办（按 ROI 排序）

1. **布局复核**：武器前出与操控者可达性权衡（当前 `preferred_weapon_cells` 偏前出）。
2. **升级链**：真机环境验证升级券（本地模拟器 stub），提升火力。
3. **围墙再试**：真实地图上围墙价值可能不同（本地合成图未体现），值得在真机验证。
4. **扩大自对弈样本**：更多 seed 降低噪声后再判断改动。

## 六、结论

- **基线达标且具竞争力**：端到端可运行、赢空对手、对朴素 Bot 过半取胜、无队伍异常、可复现。
- **存活时长仍是瓶颈**：满生存分 550，当前均分 ~89，说明后期仍会被浪潮淹没。
- 本报告作为持续调优起点；每次改动跑 `self_play.py` 对比胜率/均分。
